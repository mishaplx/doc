// /** ПРОВЕРИТЬ: ЯВЛЯЕТСЯ ЛИ ОБЪЕКТ ПРОМИСОМ */
// export const isPromise = (p: any): boolean => (typeof p === 'object' && typeof p.then === 'function');

// /** ПРЕОБРАЗОВАТЬ ЗНАЧЕНИЕ В BOOL */
// export const strToBool = (val: string): boolean => val === 'true';

// /** ПРЕОБРАЗОВАТЬ ЗНАЧЕНИЕ В INT */
// export const strToInt = (val: string): number => {
//   const val2 = Number(val);
//   return isNaN(val2) ? undefined : val2;
// }
